---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: johnpapa/angular-cli

inputs:
- name: marcus-angular

run:
  path: marcus-angular/pipelines/tasks/run-build.sh
  args:
    - -exc

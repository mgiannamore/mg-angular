---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalpa/angular-cli

inputs:
- name: marcus-angular

run:
  path: sh
  args:
    - -exc
    - |
      chmod 755 tasks/run-build.sh
      tasks/run-build.sh
